<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Agendamento Confirmado - Barbearia Elite</title>
    <link rel="stylesheet" href="/css/agendamento.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&family=Roboto:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
   
  </head>
  <body>
    <nav class="navbar">
      <div class="nav-container">
        <a href="/" class="logo">
          <span class="logo-icon">💈</span> Barbearia Elite
        </a>
        <div class="nav-links">
          <a href="/" class="nav-link">🏠 Início</a>
        </div>
      </div>
    </nav>

    <main class="confirmation-page-container">
      <div class="confirmation-box">
        <div class="success-header">
          <span class="success-icon-large">✅</span>
          <h2>Agendamento Confirmado!</h2>
        </div>

        <p>
          Parabéns, <strong><%= nome %></strong>! Seu agendamento foi realizado
          com sucesso.
        </p>

        <div class="details-section">
          <h3>Detalhes do Agendamento:</h3>
          <p><strong>Serviço:</strong> <%= servico %></p>
          <p><strong>Data:</strong> <%= data %></p>
          <p><strong>Horário:</strong> <%= hora %></p>
          <p style="margin-top: 1rem; margin-bottom: 0.5rem">
            <strong>ID do Agendamento:</strong>
          </p>
          <div class="id-container">
            <code id="appointmentId"><%= id %></code>
            <button class="copy-btn" onclick="copyToClipboard()">
              Copiar ID
            </button>
          </div>
        </div>
 <p>⚠️Guarde este ID caso precise cancelar seu agendamento⚠️.</p>
        <div class="action-section">
          <h3>Precisa de ajuda? Fale conosco 📩</h3>
          <div class="action-buttons-group">
            <a
              href="https://wa.me/5511999999999?text=Ol%C3%A1!%20Tenho%20uma%20d%C3%BAvida%20sobre%20meu%20agendamento%20de%20<%= data %>%20%C3%A0s%20<%= hora %>.%20ID:%20<%= id %>"
              target="_blank"
              class="whatsapp-btn"
            >
              📲 Falar no WhatsApp
            </a>

            <form id="cancelamentoForm" style="width: 100%">
              <input type="hidden" name="id_cancelar" value="<%= id %>" />
              <button type="submit" class="cancel-appointment-btn">
                ❌ Cancelar Agendamento
              </button>
            </form>
          </div>
        </div>
      </div>
    </main>
    <script>
      function copyToClipboard() {
        const idElement = document.getElementById("appointmentId");
        const button = document.querySelector(".copy-btn");
        navigator.clipboard
          .writeText(idElement.innerText)
          .then(() => {
            button.innerText = "Copiado!";
            setTimeout(() => {
              button.innerText = "Copiar ID";
            }, 2000);
          })
          .catch((err) => {
            console.error("Erro ao copiar: ", err);
            alert("Erro ao copiar. Por favor, copie manualmente.");
          });
      }

      const formCancelamento = document.getElementById("cancelamentoForm");
      if (formCancelamento) {
        formCancelamento.addEventListener("submit", async (e) => {
          e.preventDefault();

          if (!confirm("⚠️Tem certeza que deseja cancelar o agendamento?⚠️")) {
            return;
          }

          const idParaCancelar = formCancelamento.querySelector(
            '[name="id_cancelar"]'
          ).value;

          try {
            const response = await fetch("/cancelar-agendamento", {
              method: "POST",
              headers: {
                "Content-Type": "application/json",
              },
              body: JSON.stringify({ id_cancelar: idParaCancelar }),
            });

            // Se a resposta da requisição não for OK, algo deu errado no servidor
            if (!response.ok) {
              window.location.href = "/cancelamento_confirmado/erro";
              return;
            }

            // Tenta processar a resposta como JSON
            const contentType = response.headers.get("content-type");
            if (contentType && contentType.indexOf("application/json") !== -1) {
              const result = await response.json();
              if (result.success) {
                window.location.href = "/cancelamento_confirmado/sucesso";
              } else {
                window.location.href = "/cancelamento_confirmado/nao_encontrado";
              }
            } else {
              // Se a resposta não for JSON (por exemplo, um redirecionamento),
              // assume que foi um sucesso e redireciona.
              window.location.href = "/cancelamento_confirmado/sucesso";
            }
          } catch (error) {
            console.error("Erro ao cancelar agendamento:", error);
            // Este bloco agora lida apenas com erros de rede ou de requisição.
            window.location.href = "/cancelamento_confirmado/erro";
          }
        });
      }
    </script>
  </body>
</html>